

describe('newUserRedirectTest', () => {
    it('should redirect person from any url to /first-steps if database does not have any users', () => {        
    })
    it('should not redirect person from any url to /first-steps if database has at least one user', () => {
    })
})